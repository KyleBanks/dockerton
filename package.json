{
  "name": "dockerton",
  "version": "1.0.0",
  "description": "Dockerton wraps the core functionality of Docker into an easy-to-use Node.js library.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --bail --recursive --timeout 2000"
  },
  "devDependencies": {
    "chai": "^3.4.1",
    "mocha": "^2.3.3"
  },
  "author": "Kyle Banks",
  "license": "MIT",
  "dependencies": {
      "bluebird": "^3.0.5"
  }
}
